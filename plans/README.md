# Implementation Plans

Generated by the improve skill on 2026-06-30. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | CLI argument/command logic is unit-tested | P1 | S | — | DONE (branch `worktree-agent-aecfdde5afd112ddf`) |

## Dependency notes

- None.

## Findings considered and rejected

- CLI `fetchPng` SSRF / arbitrary URL: by-design — it is a CLI fetching the user's own OG route.
- Header case-collision in `createOgRoute`: not a bug — the spread lands last in the object literal and `Headers` dedups case-insensitively (last write wins), so caller overrides apply correctly.
- Surrogate-pair truncation in `normalizeText` `.slice()`: real but trivial for OG text; satori tolerates it. Not worth a plan.
- No ESLint (lint = `tsc --noEmit`): deliberate minimalism per CONTRIBUTING; leave it.
- Theme/variant preset system (direction): risks the "renderer abstraction" CONTRIBUTING explicitly warns against; not recommended.

## Deferred direction option (not a defect)

- Custom font helper for `next/og` (CJK/Arabic/emoji support). Highest-value feature addition, stays within the project's narrow charter. Would need its own design/spike plan if the maintainer wants to grow scope.
